//WAP in to convert a negative value in positive no using abs() method
#include<stdio.h>
main()
{
	int a;
	printf("Enter a negative no. : ");
	scanf("%d",&a);
	printf("After converting value is : %d",abs(a));
}
